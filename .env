# .env — Fill with real values. DO NOT commit this file to version control.

# Database (PostgreSQL)
# Example: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
	# Example local: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
	# Example Neon (replace with values from the Neon dashboard):
	# postgresql://<USER>:<PASSWORD>@ep-xxxxxxx.us-east-1.aws.neon.tech:5432/<DBNAME>?sslmode=require
	# When using Neon, copy the provided "Client"/connection string and paste it below.
DATABASE_URL= postgresql://neondb_owner:npg_QTKwotrec1y3@ep-long-mountain-aeuk7b16-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# NextAuth / Auth
	# NextAuth / Auth
	# Public app URL (used when building links in emails). Set both for compatibility.
NEXTAUTH_URL=https://cookingblogofivo.netlify.app
AUTH_URL=https://cookingblogofivo.netlify.app
	# NextAuth secret (use a strong random string)
NEXTAUTH_SECRET=replace-with-a-long-random-secret
AUTH_SECRET=replace-with-a-long-random-secret

# Mailer (nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass

# Sentry (optional)
SENTRY_DSN=
