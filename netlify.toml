[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Note: Upstash integration removed — configure any environment variables
# in the Netlify UI: Site settings -> Build & deploy -> Environment.
